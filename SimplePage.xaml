<Page
    x:Class="O365_Win_Profile.SimplePage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:O365_Win_Profile"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Width="400" Height="800"
    DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Right">
            <TextBlock x:Name="UserName" Text="Tester" HorizontalAlignment="Right" FontSize="30" />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Vertical">
            <Button Name="btnClick" IsEnabled="False" HorizontalAlignment="Left" Content="Choose from Webcam" Width="300" Height="35" BorderBrush="Transparent" Foreground="White" Background="#F18092" VerticalAlignment="Top" Margin="0, 0, 0, 0" Click="Identify_Click"/>
            <Grid Width="300" HorizontalAlignment="Left">
                <Canvas Name="MyCanvas" HorizontalAlignment="Left" Height="300" Width="300"></Canvas>
                <Image  HorizontalAlignment="left" Source="{Binding SelectedFile}" VerticalAlignment="Center" Height="300" Width="300" x:Name="ImageDisplay" />
            </Grid>
            <TextBox IsReadOnly="True" Name="outputtxtbox" Width="300" Height="220" HorizontalAlignment="Left"  Text="{Binding Output}" Padding="8" TextWrapping="Wrap"  Background="White" ></TextBox>
            <Button Name="loginbtn" Height="95" Width="300" Click="loginbtn_Click">Login</Button>
        </StackPanel>
        <ListBox x:Name="listBox" HorizontalAlignment="Left" Height="569" Margin="422,71,-371,0" Grid.Row="1" VerticalAlignment="Top" Width="349"/>

    </Grid>
</Page>
